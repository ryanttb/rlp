# Environment Configuration for RLP Application
# Copy this file to .env.local and fill in your actual values

# Firebase Configuration (Legacy - for migration from Firebase to GCP)
# These are only needed if you're still using Firebase features
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Optional: Enable Firebase Analytics
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# GCP Configuration (Primary - for PostgreSQL and Cloud Storage)
# Database URL for PostgreSQL (Cloud SQL)
# Get the private IP from: terraform output database_private_ip
# Get the password from your Terraform variables
DATABASE_URL=postgresql://rlp_user:your_actual_password@your_private_ip:5432/rlp_database

# GCP Project Configuration
# Get these values from your Terraform configuration
GCP_PROJECT_ID=your-actual-gcp-project-id
GCP_REGION=us-central1
GCP_ZONE=us-central1-a

# Cloud Storage Configuration
# Get this from: terraform output storage_bucket
GCP_STORAGE_BUCKET=your-project-id-static-assets

# Google Cloud Service Account (for production)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json 